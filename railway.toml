[build]
builder = "nixpacks"
buildCommand = "pip install -e . && cd frontend && npm install && npm run build"

[deploy]
startCommand = "python -m backend.main"
healthcheckPath = "/health"
healthcheckTimeout = 100

[env]
PORT = "8001"
